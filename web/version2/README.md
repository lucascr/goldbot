Version 2 dashboard workspace.

This folder holds a separate copy of the current dashboard frontend so we can redesign and experiment without overwriting the main `web/templates/` and `web/static/` files again.

Current files:
- `templates/dashboard.html`
- `static/dashboard.css`
- `static/dashboard.js`
